gitops-runtime:
  # gitops-operator:
    # image:
      # tag: v0.3.8
  app-proxy:
    # image:
    #   tag: 1.3125.0-CR-25542-improve-git-locks-32001fd
    #   pullPolicy: Always
    config:
      cors: http://local.codefresh.io,https://akavalchuk-platform.ngrok.io
      graphqlPlayground: 'true'
  global:
    codefresh:
      url: https://akavalchuk-platform.ngrok.io
      userToken:
        secretKeyRef:
          name: codefresh-user-token
          key: token
    runtime:
      cluster: https://kubernetes.default.svc
      codefreshHosted: false
      ingress:
        className: nginx-internal
        enabled: true
        hosts:
          - akavalchuk-local.rnd-sandbox.cf-infra.com
        protocol: https
      name: akav-demo-helm-runtime-2
      isConfigurationRuntime: true
  installer:
    skipValidation: true
