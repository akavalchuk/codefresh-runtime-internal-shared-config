apiVersion: codefresh.io/v1beta2
kind: Product
metadata:
  name: simple-product
spec:
  promotionFlows:
    - name: bruh
      gitTriggerSelectors:
        - key: commitMessage
          operator: In
          values:
            - test-empty
  concurrency: queue
