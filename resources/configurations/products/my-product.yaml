apiVersion: codefresh.io/v1beta2
kind: Product
metadata:
  name: my-product
spec:
  promotionFlows:
    - name: no-pf-test
      gitTriggerSelectors:
        - key: commitMessage
          operator: In
          values:
            - test-empty
    - name: bruh
      gitTriggerSelectors:
        - key: commitMessage
          operator: In
          values:
            - bruh
  promotionTemplate:
    promotion:
      guestbook-ui-deployment.yaml:
        jsonPaths:
          - $.*.*.*.*.*.image
  concurrency: terminate
